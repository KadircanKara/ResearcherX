/** The app's own "R" tile, so the page and the product share one mark. */
export function LogoMark() {
  return (
    <span
      aria-hidden
      className="bg-site-accent text-site-accent-fg grid size-7 place-items-center rounded-md text-xs font-bold"
    >
      R
    </span>
  );
}
